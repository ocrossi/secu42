#include <stdio.h>
#include <string.h>
#include <stdlib.h>

void *auth;
void *service;
// void *reset;
// void *login;
//

int main(void) {
	char buf[128]

	while (1) {
		printf("%p, %p \n", auth, service);	
		if (fgets(buff, 128, STDIN) == 0)
			return (0);
		// v1 = 5;
		// s = "auth ";

		if (strncmp("auth ", buffer, 5) == 0) {
			malloc(4);
		}
	}			
	return (0);
}
